#include <stdio.h>

int main(){
    printf("size of pointer to integer : %d\n",sizeof(char*));
    printf("size of pointer to integer : %d\n",sizeof(int*));
    printf("size of pointer to integer : %d\n",sizeof(float*));
    printf("size of pointer to integer : %d\n",sizeof(double*));
    return 0;
}